{
  "name": "two-step-form",
  "version": "1.0.0",
  "description": "a two step form built with React and Node",
  "main": "index.js",
  "scripts": {
    "start": "npm run-script start:server & npm run-script start:client",
    "start:server": "node index.js",
    "start:client": "cd client && npm start",
    "dev": "nodemon index.js & npm run-script start:client",
    "build": "npm run-script build:server & npm run-script build:client",
    "build:client": "cd client && npm install && npm run-script build",
    "build:server": "npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "alex hughes",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "lodash": "^4.17.11"
  },
  "devDependencies": {
    "nodemon": "^1.18.10"
  }
}
